@model IEnumerable<WebApplication.Models.DBContent>

<div id="searchresults">
    <h3>Ответы от сервиса delay/x:</h3>
    <ul>
        @foreach (var item in Model)
        {
            <li>@item.DelayedResponseTime</li>
        }
    </ul>
</div>
